services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: refract
      POSTGRES_PASSWORD: refract
      POSTGRES_DB: refract
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
